<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- 该处在弹出框里的 -->
     <style  
        name="CustomProgressDialog"  
        parent="@android:style/Theme.Dialog">  
        <item name="android:windowFrame">@null</item><!--边框-->  
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->  
        <item name="android:windowIsTranslucent">@android:color/transparent</item><!--半透明-->  
        <item name="android:windowNoTitle">true</item><!--无标题-->  
        <item name="android:backgroundDimEnabled">true</item>
  	
        <!-- 更换背景图片可以实现全透明全透明 @android:color/transparent -->
        <item name="android:windowBackground">@drawable/select_dialog_bg</item>
        <item name="android:backgroundDimAmount">0.6</item>  
    </style>
     
    <!-- 该处在首页tab按钮里 -->
	<style name="main_tab_bottom"> 
	     <item name="android:textSize">20sp</item> 
	     <item name="android:textColor">#000000</item> 
	     <item name="android:gravity">center_horizontal|center_vertical</item> 
	<!-- 该处引用drawable下面的一个xml文档(selector) --> 
	     <item name="android:layout_width">fill_parent</item> 
	     <item name="android:layout_height">fill_parent</item> 
	     <item name="android:layout_weight">1.0</item> 
	<!-- 每个按钮的权重，相等则平分尺寸 --> 
	     <item name="android:button">@null</item> 
	<!-- 隐藏单选按钮 --> 
	     <item name="android:layout_marginTop">1.0dip</item> 
	 </style>     
</resources>
